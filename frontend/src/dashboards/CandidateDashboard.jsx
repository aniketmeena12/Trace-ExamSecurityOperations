import { Card } from "../components/primitives";

// Implemented in a later commit.
export function CandidateDashboard() {
  return (
    <Card title="Candidate Portal" subtitle="coming online">
      <p className="text-sm text-muted">Candidate portal initializing…</p>
    </Card>
  );
}
