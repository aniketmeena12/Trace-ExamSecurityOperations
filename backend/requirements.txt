# Trace backend dependencies (grows per milestone).
# M1 — crypto core:
pycryptodome>=3.20.0
pytest>=8.0

# M2 — API, auth, audit, time gate:
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pyjwt>=2.8
python-multipart>=0.0.9
httpx>=0.27          # for FastAPI TestClient

# M3 — DCT watermarking:
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2

# Docs — generate the project documentation (scripts/build_docs.py):
python-docx>=1.1

# Production:
psycopg2-binary>=2.9      # PostgreSQL adapter
gunicorn>=21.2            # Production WSGI server for async
python-dotenv>=1.0        # Environment variable management
