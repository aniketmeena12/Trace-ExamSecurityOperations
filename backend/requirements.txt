# Trace backend dependencies (grows per milestone).
# M1 — crypto core:
pycryptodome>=3.20.0
pytest>=8.0

# M2 — API, auth, audit, time gate:
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pyjwt>=2.8
python-multipart>=0.0.9
httpx>=0.27          # for FastAPI TestClient

# M3 — DCT watermarking:
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2
